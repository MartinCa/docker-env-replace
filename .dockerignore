# Keep the Docker build context small and the image free of files the
# builder and runtime stages do not need.
.git
.github
examples
docs
*.md
docker-compose.yml
.env
.env.*
.docker-env-replace-tmp-*
docker-env-replace