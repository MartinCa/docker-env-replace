# docker-env-replace demo: normal replacement.
# Set DB_HOST and DB_PORT in the environment before running.
host = <DB_HOST>
port = <DB_PORT>

# Tokens may appear anywhere, multiple times, and mixed with text.
url = http://<DB_HOST>:<DB_PORT>/api/<DB_HOST>
